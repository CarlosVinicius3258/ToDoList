import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
    writeTaskWrapper: {
        position: 'absolute',
        top: 110,
        flexDirection: 'row',
        width: '100%'
    }
})

export default styles;